# Checks
- Read Swinney et al. paper in docs/background for image analysis methods and measurement definitions referenced in Figure 2.
- Confirm that app does not include spines for RW, CL, or CW.
- Confirm that app does include spines for OW.
- Confirm that SL is the length of the first spine. 

# Automate
- Create a plan to automate image processing and minimize the number of user clicks required.
- Evaluate the suitability of the plan using the full range of high-quality and low-quality images in data/examples.
Produce a proposed workflow.

# Institute phase 1
- Go ahead with phase 1 now.
- Accept all required bash commands.

# Run on Windows  
- Provide step-by-step instructions for a naive user to load and run on Windows.
- Assume only that Python is loaded on the machine.
- Confirm that the user will be prompted to identify the location of the input images on the local macine, as well as the location of .csv output files.
- Write a user's guide aimed at naive users and push it as a .pdf to the docs folder.